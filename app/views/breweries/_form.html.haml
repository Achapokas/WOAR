= form_for @brewery do |f|
  - if @brewery.errors.any?
    #error_explanation
      %h2= "#{pluralize(@brewery.errors.count, "error")} prohibited this brewery from being saved:"
      %ul
        - @brewery.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
